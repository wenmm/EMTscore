% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AUCell_multiple_genesets.R
\name{Execute_AUCell_parallel}
\alias{Execute_AUCell_parallel}
\title{Compute AUCell Scores for Multiple Gene Sets}
\usage{
Execute_AUCell_parallel(exprMatrix, gmt_file, cores)
}
\arguments{
\item{exprMatrix}{Numeric matrix or data frame of gene expression, where row names are genes and column names are samples.}

\item{gmt_file}{Character string. Path to the GMT file containing gene sets.}

\item{cores}{Integer. Number of CPU cores to use for parallel processing.}
}
\value{
A data frame containing AUCell scores for each pathway across samples.
}
\description{
This function computes AUCell scores for each gene set in a GMT file
using a given gene expression matrix (genes as rows, samples as columns).
It runs in parallel across multiple cores and returns a combined data frame
of AUCell scores for all pathways.
}
\examples{
url <- "https://zenodo.org/records/17438655/files/geneExp.rda"
destfile <- tempfile(fileext = ".rda")
download.file(url, destfile, mode = "wb")
load(destfile)
gmt_file <- system.file("extdata", "h.all.v2025.1.Hs.symbols.gmt", package = "EMTscore")
result <- Execute_AUCell_parallel(geneExp, gmt_file, cores = 2)
}
