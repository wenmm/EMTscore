% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EM_state_classification.R
\name{predict_cluster_labels}
\alias{predict_cluster_labels}
\title{Predict Cluster Labels from PCA Results Using GMM or Kmeans}
\usage{
predict_cluster_labels(
  pca_result,
  method = "GMM",
  n_clusters = 3,
  PC_name = c("Escore", "Mscore")
)
}
\arguments{
\item{pca_result}{A numeric matrix or data frame of PCA results (rows = samples, columns = PCs).}

\item{method}{Character, either "GMM" (Gaussian Mixture Model) or "Kmeans" for clustering. Default is "GMM".}

\item{n_clusters}{Numeric, number of clusters for Kmeans. Ignored if method = "GMM".}

\item{PC_name}{Character vector of length 2 specifying the column names for Escore and Mscore. Default is c("Escore", "Mscore").}
}
\value{
A named character vector of cluster labels for each sample.
}
\description{
This function clusters PCA results using Gaussian Mixture Model (GMM) or K-means,
identifies the clusters with maximum Escore and Mscore, assigns labels "E", "M",
and "EM1", "EM2", ... to other clusters, and returns a named vector of final cluster
labels for each sample.
}
\examples{
data(nnPCA_Result_multiple)
labels_gmm <- predict_cluster_labels(nnPCA_Result_multiple, method = "GMM", n_clusters = 3, PC_name = c("Escore", "Mscore"))
labels_km  <- predict_cluster_labels(nnPCA_Result_multiple, method = "Kmeans", n_clusters = 3,PC_name = c("Escore", "Mscore"))
}
