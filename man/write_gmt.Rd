% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_gmt_from_genelists.R, R/utils.R
\name{write_gmt}
\alias{write_gmt}
\title{Write Gene Sets to a GMT File}
\usage{
write_gmt(gene_sets, file)

write_gmt(gene_sets, file)
}
\arguments{
\item{gene_sets}{A named list where each element is a character vector of gene identifiers.
The list names correspond to gene set names.}

\item{file}{A character string specifying the path to the output GMT file.}
}
\value{
No return value. The function writes a GMT file to the specified path.

This function writes a GMT file to disk and returns \code{NULL} invisibly.
}
\description{
Convert multiple gene lists into a single GMT (Gene Matrix Transposed) formatted file.  
Each element of the input list represents one gene set, where the element name  
is the gene set name and the element value is a vector of gene symbols.

This function exports a list of gene sets into a GMT (Gene Matrix Transposed) file format.  
Each line of the GMT file corresponds to one gene set, with the structure:
By default, the gene set name is used as both the name and description fields.
}
\details{
The GMT format is widely used by tools such as GSEA and Enrichr for representing gene set collections.
Each line contains the set name, a description (often a URL or repeated name), and one or more gene identifiers separated by tabs.
}
\examples{
gene_sets <- list(
  EMT_High = c("VIM", "ZEB1", "SNAI1"),
  EMT_Low  = c("CDH1", "OCLN", "DSP")
)
write_gmt(gene_sets, "EMT_signature.gmt")

gene_sets <- list(
  E = c("CDH1", "VIM", "SNAI1", "ZEB1"),
  M = c("CDK1", "CCNB1", "CDC20")
)
write_gmt(gene_sets, file = "EM_signature.gmt")

}
